{
    "name": "go-wasm-calc",
    "main": "index.ts",
    "scripts": {
        "prebuild": "npm install",
        "build": "esbuild index.ts --bundle --outfile=bundle.js --sourcemap"
    },
    "devDependencies": {
        "esbuild": "0.27.2",
        "typescript": "5.9.3"
    }
}
